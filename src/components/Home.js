import React from 'react';
import SobreMim from './SobreMim'; // Ajuste o caminho se necessário
import Contato from './Contato';   // Ajuste o caminho se necessário

const Home = () => (
  <div className="App">
    <SobreMim/>
    <Contato/>
  </div>
);

export default Home;